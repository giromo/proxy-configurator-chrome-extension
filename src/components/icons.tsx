import { LucideIcon } from "lucide-react";
import { ChevronDown, Check, Loader2, Info, CircleCheck, CircleX, CircleDot, RefreshCw } from "lucide-react";

export const Icons = {
  chevronDown: ChevronDown,
  check: Check,
  loader2: Loader2,
  info: Info,
  circleCheck: CircleCheck,
  circleX: CircleX,
  circleDot: CircleDot,
  refreshCw: RefreshCw,
} as const;
